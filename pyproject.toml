[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "devitocurvilinear"
version = "0.1.0"
dependencies = ["numpy"]

[tool.setuptools.packages.find]
include = ["devitocurvilinear*"]
